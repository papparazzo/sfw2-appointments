<tr id="{{formid}}_recordset_{{id}}">
    <td style="text-align:left; padding-left: 5px;">{{startDay}}</td>
    <td style="text-align:left; padding-left: 5px;">{{startDate}}</td>
    <td>{{startTime}} Uhr</td>
    <td>{{home}}</td>
    <td>{{guest}}</td>
    {{#if deleteAllowed}}
    <td style="width: 100px;">
        <input type="hidden" id="{{formid}}_title_{{id}}" value="{{startDate}} ; {{startTime}} Uhr {{home}} - {{guest}}" />
        <button
            type="button"
            class="btn btn-danger btn-sm"
            data-toggle="modal"
            data-target="#deleteDialogModal"
            data-url="{{url}}"
            data-item-id="{{id}}"
            data-form-id="{{formid}}"
        >
            löschen
        </button>
    </td>
    {{/if}}
</tr>
